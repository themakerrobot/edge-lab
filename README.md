# 엣지 랩 (Edge Lab)

**인터넷 없이, 내 컴퓨터 안에서 도는 AI 학습 도구.**

사진을 찍어 AI에게 물어보고, 블록과 파이썬으로 AI를 부르는 프로그램을 만들고,
내가 직접 AI를 가르쳐 봅니다. 설치가 끝나면 **인터넷을 끊어도 전부 그대로 동작**하고,
찍은 사진과 만든 것은 이 PC 밖으로 나가지 않습니다.

Intel AI PC의 CPU·GPU·NPU에 일을 나눠 실행합니다.

---

## 화면

위쪽 탭으로 옮겨 다닙니다. 지금 보고 있는 탭에 밑줄이 그어집니다.

### 체험하기
AI 16가지를 눌러 보는 곳입니다. 웹캠으로 찍거나 사진을 올리고, 스케치북에 그려서 물어봐도 됩니다.
**실시간**을 켜면 웹캠을 보면서 계속 알아봅니다.

| 묶음 | 할 수 있는 것 |
|---|---|
| 얼굴 | 얼굴 찾기 · 나이/성별 · 감정 · 거리와 방향 · 마스크를 썼는지 |
| 몸 · 손 | 사람 자세(관절) · 손동작 |
| 사물 | 사물 찾기 · 사물 분할(색칠) |
| 말로 답하는 AI | 사진을 보고 묻는 말에 답하기 |
| 사진 바꾸기 | 배경 지우기 · 화질 4배 개선 · 깊이 지도(멀고 가까움을 색으로) |
| 글자 · QR | 글자 읽기 · QR 읽기 |

### 블록
블록을 끼워 맞춰 AI를 움직입니다. [예제]로 만들어진 프로그램을 불러와 바로 실행해 볼 수 있고,
만든 작품은 저장했다가 다시 엽니다.

이미지 · AI 인식 · 소리 · AI 언어 · 이벤트 블록과, 논리 · 반복 · 수학 · 텍스트 · 목록 · 변수 · 함수
같은 기본 블록이 있습니다. **내가 만든 블록이 파이썬으로 어떻게 되는지** 옆에서 볼 수 있고,
그대로 파이썬 화면으로 넘길 수도 있습니다.

### 파이썬
글로 코드를 써서 AI를 부릅니다. `themaker` 라이브러리로 한 줄이면 됩니다.

```python
from themaker import *

photo = camera()                      # 웹캠으로 한 장
print(vision("object", photo))        # 무엇이 보이나
show(vision("depth", photo))          # 멀고 가까움을 색으로
speak("안녕하세요")                     # 말하기
```

예제 16종과 도움말이 들어 있고, 자동완성이 함께 뜹니다.

### 가르치기
내가 직접 AI를 가르칩니다. 구별할 종류를 만들고(예: 가위·바위·보), 웹캠으로 예시 사진을 모아
학습시키면 바로 시험해 볼 수 있습니다. 사진 대신 **손 모양 · 표정 · 상반신 · 전신**으로도 배웁니다.

얼마나 잘 배웠는지 정확도와 학습 곡선으로 보여 주고, 틀린 사진도 함께 보여 줍니다.
만든 AI를 저장하면 **체험하기 · 블록 · 파이썬에서 그대로 불러 씁니다.**

### 대화
AI와 이야기합니다. 마이크로 말하거나 글로 쓰고, **읽어주기**를 켜면 목소리로 답합니다.
목소리와 말투를 고를 수 있습니다.

**내 자료를 넣어 두면 그 안에서 찾아 답합니다.** 수업 자료를 붙여 넣고 물어보면
어디서 가져왔는지도 함께 보여 줍니다.

### 설정
수업 전에 **카메라 · 소리 · 마이크**가 되는지 확인합니다. 마이크 시험은 3초 녹음해 그대로 되들려
주므로 마이크와 스피커가 한 번에 확인됩니다.

이름을 적어 두면 결과물에 함께 남고, 밝은 곳에서 화면이 안 보이면 **또렷하게 보기**로 바꿉니다.
학습 결과와 사용 기록도 여기서 봅니다.

---

## 처음 켜면

파이보가 말풍선으로 화면을 한 바퀴 안내합니다. 헤더의 **?** 를 누르면 언제든 다시 볼 수 있습니다.
오른쪽 위 **한/영** 으로 언어를 바꿉니다.

**웹캠은 없어도 됩니다.** 카메라가 없거나 다른 프로그램이 쓰고 있어도 화면은 그대로 돌아갑니다 —
사진을 올려서 하면 됩니다.

---

## 내가 만든 것은 어디에

작품과 기록은 프로그램 폴더가 아니라 **`문서\Edge Lab`** 에 따로 저장됩니다.
새 버전으로 덮어써도 사라지지 않습니다.

| 폴더 | 들어 있는 것 |
|---|---|
| `user` | 가르친 AI |
| `blocks` | 블록 작품 |
| `pycode` | 파이썬 작품 |
| `db` | 대화에 넣은 자료 |
| `models` | 가져다 둔 모델 파일(.pt) |
| `stats` | 사용 기록 · 학습 결과 |

설정의 **[폴더 열기]** 로 탐색기에서 바로 열립니다.

**USB로 들고 다니기** — 이 폴더를 통째로 복사해 두고, 다른 PC에서 설정의 **[바꾸기]** 로
그 폴더를 고르면 이름·작품·기록까지 그대로 이어서 씁니다.

**전체 초기화**는 작품·자료·기록만 지웁니다. AI 모델은 그대로 남습니다.

---

## 설치 · 실행

설치는 [INSTALL.md](INSTALL.md) 를 봅니다. 코드를 고칠 때 규칙은 [DEVELOP.md](DEVELOP.md) 에 있습니다.

설치가 끝나면:

```
run.bat
```

브라우저가 자동으로 열리고, 모델을 올리는 동안 진행 상황을 보여 줍니다.
처음 켤 때는 1~2분 걸리고, 그 다음부터는 빠릅니다.

---

## 쓰는 모델

| 하는 일 | 모델 | 도는 곳 |
|---|---|---|
| 사진 질문 · 대화 | Gemma 3 4B INT4 | GPU |
| 사물 · 자세 · 분할 | YOLO11m (+pose/seg) | CPU |
| 마스크 | YOLO11s-cls | CPU |
| 얼굴 검출 · 나이/성별 · 감정 · 방향 | OpenVINO 프리트레인 | NPU |
| 얼굴 거리 · 손동작 · 표정 학습 | MediaPipe | CPU |
| 깊이 지도 | Depth Anything V2 Small | GPU |
| 배경 제거 · 화질 개선 | U2Net · SR-1032 | GPU |
| 글자 인식 | easyocr (한/영) | CPU |
| QR 인식 | OpenCV QRCodeDetector | CPU |
| 가르치기 특징 추출 | MobileNetV2 1280d | NPU |
| 말을 글자로 | Whisper Small INT8 | CPU |
| 글자를 말로 | Supertonic 3 | CPU |

---

## 라이선스
이 프로젝트는 **AGPL-3.0** 으로 배포한다 (`LICENSE`). YOLO(ultralytics, AGPL-3.0)를 포함하므로
전체를 같은 조건으로 공개하며, 소스·모델을 받아 사용/수정/재배포할 수 있다.

포함된 구성 요소:
| 구성 요소 | 라이선스 |
|---|---|
| Gemma 3 | Gemma Terms of Use |
| YOLO11 (ultralytics) | AGPL-3.0 |
| OpenVINO / open_model_zoo 얼굴 모델 | Apache-2.0 |
| MediaPipe | Apache-2.0 |
| U2Net | Apache-2.0 |
| easyocr | Apache-2.0 |
| Whisper (OpenVINO 변환본) | Apache-2.0 |
| Supertonic 3 (모델 가중치) | OpenRAIL-M (BigScience Open RAIL-M) |
| Supertonic 추론 절차 (speech_routes.py) | 공식 예제(supertone-inc/supertonic) MIT 기반 이식 |
| Blockly / TensorFlow.js / JSZip | Apache-2.0 / Apache-2.0 / MIT |

### TTS 모델(OpenRAIL-M) 재배포 시 지켜야 할 것
상업 사용·재배포·서비스 호스팅은 허용된다. 다만 모델(또는 그 파생물)을 남에게 넘길 때:
1. 라이선스 사본(`models/tts/LICENSE`)을 함께 준다.
2. 이용 약관·계약서에 **Attachment A의 사용 제한**을 그대로 넣고, 받는 쪽에 그 적용을 알린다.
3. 모델 파일을 고쳤다면 고쳤다는 표시를 남긴다.

주요 제한(발췌): 미성년자 착취·가해, 허위정보 유포, 동의 없는 인물 사칭(딥페이크),
기계 생성물임을 밝히지 않은 배포, 의료 조언·판독, 법집행·출입국 자동판정 등.
이 제한은 AGPL 코드가 아니라 **TTS 모델 가중치에만** 적용된다 (코드와 모델은 별개 저작물).
