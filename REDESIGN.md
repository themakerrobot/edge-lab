# 웹 화면 재설계 — 검토와 기획

목표 세 가지.

1. **공간을 넓게** — 겹쳐 쌓인 가로 막대를 없애 작업 공간을 키운다.
   VS Code 의 *배치*를 가져오되 *치수*는 가져오지 않는다 — §2.0.
2. **런처(웹OS)** — 큰 아이콘이 깔린 홈에서 앱을 하나씩 골라 실행하는 흐름.
3. **브랜드 통일** — 한국어 화면에서도 `edge-lab` 으로 보인다.

**전제: 쓰는 사람은 초등학생이다.** 교실 노트북 트랙패드로, 밝은 교실에서, 처음 보는 화면을 쓴다.
누르는 것은 크고, 글자는 크고, 아이콘 옆에는 늘 글자가 있어야 한다.
치수 기준은 §2.0 에 모았고 그 값이 나머지 모든 수치를 지배한다.

이 문서는 **기획만** 담는다. 구현은 아래 "단계" 순서대로 따로 커밋한다.

---

## 1. 지금 상태 (전 페이지 검토)

### 1.1 파일 구성

| 파일 | 크기 | 밀도 | 화면 뼈대 |
|---|---|---|---|
| `index.html` (체험하기) | 92 KB | explore | 헤더 + 문항1 바 + 문항2 바 + 패널 2 |
| `blocks.html` (블록) | 175 KB | work | 헤더 + 패널 2 (`data-split`) |
| `code.html` (파이썬) | 86 KB | work | 헤더 + 패널 3 (`data-split`, 도움말은 접힘) |
| `train.html` (가르치기) | 99 KB | explore | 헤더 + 패널 2 |
| `talk.html` (대화) | 42 KB | explore | 헤더 + 패널 2 |
| `options.html` (설정) | 57 KB | explore | 헤더 + 패널 2 |

공용 레이어 — `lib/ui.css`(헤더·패널 뼈대·간격 토큰), `lib/nav.js`(헤더 글자 탭 6개 주입),
`lib/sysbar.js`(우상단 시스템 오버레이), `lib/split.js`(끌어서 크기 조절),
`lib/tour.js`(파이보 튜토리얼), `lib/usage.js`(사용 통계).

여섯 페이지 모두 **인라인 `<style>` + 인라인 `<script>` 단일 파일**이다.

### 1.2 공간 — "꽉 차게"는 이미 되어 있다. 문제는 세로 크롬이다

전 페이지가 이미 `height:100dvh; overflow:hidden; display:flex; flex-direction:column` 이고
`.main-container` 는 `flex:1 1 auto`. 즉 **좌우는 이미 full-bleed 이고 최대폭 제한도 없다.**
"좁아 보이는" 진짜 원인은 가로가 아니라 **머리 위에 쌓인 가로 막대들**이다.

체험하기(`index.html`) 기준 세로 크롬 계산:

| 요소 | 높이 |
|---|---|
| `.header` (버튼 34px + 패딩 12 + 테두리 4) | ≈ 50 px |
| `.subheader` — 문항 1 "모델을 골라요" | ≈ 45 px |
| `.tab-prompt-row` — 문항 2 "이미지를 넣고 실행해요" | ≈ 53 px |
| `body` 세로 gap 3칸 (`--sp-2`) | ≈ 21 px |
| `body` 상하 패딩 (`--edge`) | ≈ 14 px |
| **합계** | **≈ 183 px** |

교실 노트북 기준(1366×768)에서 **세로의 24%가 내용 이전에 사라진다.**
블록·파이썬은 헤더 + `.panel-head` 로 ≈ 90px 이라 사정이 낫지만, 그래도 두 겹이다.

### 1.3 헤더가 좁은 화면에서 두 줄이 된다

`.header` 는 `flex-wrap: wrap`, `.h-tools` 안에는 **탭 6개 + 알약 3개(`?`, `한/영`, `AI`)** 가 들어간다.
한국어 탭 라벨(체험하기·블록·파이썬·가르치기·대화·설정) 합계 폭 + 알약 3개 + 로고·제목이
1280px 안팎에서 한 줄을 넘긴다. 넘기는 순간 헤더가 **50px → 92px** 로 커진다.
가로로 늘어놓는 탭 줄은 폭이 줄면 반드시 세로를 먹는 구조다.

### 1.4 반응형 — 기준이 없다

페이지마다 브레이크포인트가 제각각이다.

| 페이지 | 브레이크포인트 |
|---|---|
| index | 560 / 860 / 1200 |
| blocks | 900 |
| code | 980 / 1200 |
| train | 860 |
| talk | 960 |
| options | 860 |

같은 창 폭에서 페이지를 옮기면 어떤 페이지는 세로로 접히고 어떤 페이지는 안 접힌다.
좁은 화면에서 `overflow:auto; height:auto` 로 "문서처럼 스크롤"로 넘어가는 페이지는
**`blocks.html` 하나뿐**이다. 나머지 다섯은 `overflow:hidden` 인 채로 눌려서, 폭이 좁아지면
내용이 잘린다.

패널 최소폭도 하드코딩이다 — `380 / 360 / 340 / 320 / 300 / 260 / 240 / 220px`.
1366px 노트북에서 파이썬 3패널(편집기 340 + 출력 300 + 도움말 260 + 손잡이)은
이미 한계라, 도움말을 열면 편집기가 코드 한 줄도 못 담는다.

### 1.5 중복 — 토큰이 6벌이다

디자인 토큰(`--paper --box --ink --cyan --line --r` …)과 밝은 곳 모드(`html.hc`) 블록이
**여섯 파일에 그대로 복사돼 있다.** `blocks.html` 은 `.header` 규칙까지 자기 CSS 에 또 갖고 있다
(`lib/ui.css` 가 뒤에 실려서 이기고 있을 뿐이다).

`lib/ui.css` 주석이 이미 이 문제를 길게 한탄하고 있다 — "페이지마다 두면 어긋난다",
"실제로 그랬다: 굵기가 써보기·대화만 600 …". 값을 ui.css 로 모으는 작업이 절반쯤 된 상태다.
**토큰은 아직 안 모였다.**

### 1.6 내비게이션 — 홈이 없다

- `/` 가 곧 체험하기다. 런처·대시보드·홈 개념이 없다.
- `nav.js` 는 아이콘을 **의도적으로 뺐다.** 주석에 이유가 적혀 있다 —
  "파이보 랩·센스 랩의 탭도 글자만이라, 셋을 나란히 열었을 때 같은 제품으로 보이려면
  여기도 글자여야 한다." → **아이콘 레일로 바꾸면 제품군 정합성이 깨진다. 결정 필요.**
- 체험하기의 AI 16종은 `<select>` 드롭다운 안에 `optgroup` 6묶음으로 숨어 있다.
  주석에도 "스물몇 개를 한 줄로 늘어놓으면 못 찾는다" 고 적혀 있다 —
  **드롭다운이 아니라 아이콘 그리드로 풀 자리다.**

### 1.7 브랜드 문자열

화면에 나오는 "엣지 랩" — 22곳.

| 위치 | 개수 |
|---|---|
| `<title>` 6페이지 | 6 |
| 헤더 `#txtTitle` 6페이지 | 6 |
| UI 사전 `titleTag` / `title` / `homeLink` / `back` | 9 |
| 부팅화면 `#bootTitle` | 1 |

화면 밖(파이썬 라이브러리·설치 스크립트) — `themaker.py` 8곳, `tools/*.bat`·`*.ps1` 4곳.
이건 콘솔·예외 메시지라 성격이 다르다. **같이 바꿀지 결정 필요.**

### 1.8 자산 — 안 쓰는 게 두 벌, 그중 하나는 라이선스 위험

- **`view_project/assets/svg/` 47개 — 어디서도 참조하지 않는다.** 완전 사장.
  그중 **42개 파일 헤더가 `Font Awesome Pro 6.4.0`** 이다.
  이 저장소는 AGPL-3.0 으로 재배포한다. Pro 아이콘 파일을 그대로 담아 배포하는 것은
  Font Awesome 상용 라이선스 위반 소지가 있다. **런처 아이콘으로 이걸 쓰면 안 된다.**
- **`view_project/lib/fa/` — Font Awesome Free 6.2.0** (`Icons: CC BY 4.0, Fonts: OFL 1.1, Code: MIT`).
  CSS + `fa-solid-900.woff2` + `fa-brands-400.woff2` 가 다 있는데 **역시 아무 페이지도 안 쓴다.**

→ 런처 아이콘은 **이미 들어 있는 FA Free 웹폰트**로 간다. Pro 헤더 SVG 47개는 삭제를 권한다.

---

## 2. 기획

### 2.0 먼저 — 아동 사용자 기준 (이 값이 모든 치수를 지배한다)

**VS Code 를 그대로 베끼면 안 된다.** VS Code 는 성인 개발자용이라 레일 48px·아이콘 16px·
글씨 12px 로 촘촘하다. 엣지 랩은 초등학생이 교실 노트북 트랙패드로 쓰는 화면이다.
"공간을 넓게" 는 **크롬을 촘촘하게 만들어서가 아니라, 겹쳐 쌓인 막대를 없애서** 얻는다.

| 항목 | 최소값 | 왜 |
|---|---|---|
| 누르는 것의 크기 | **48 × 48 px** | 애플 44 · 구글 48 이 성인 기준이다. 아이는 소근육 조절이 덜 돼 더 커야 한다 |
| 누르는 것 사이 간격 | **8 px 이상** | 붙어 있으면 옆 것이 눌린다 — 되돌리기 어려운 단추(전체 지우기)는 특히 |
| 본문 글씨 | **14 px 이상** | 지금 `.hint` 가 `.84rem`(≈13.4px), 로그가 `.82rem`(≈13.1px) 이라 이미 아래다 |
| 앱·모델 이름 | **16 px** | 런처 타일에서 읽고 고르는 글자 |
| 곁글·단위 | **12 px 이상** | 지금 `0.82rem` 짜리들이 여기 걸린다 |
| 아이콘 | **글자와 반드시 함께** | 저학년은 아이콘만으로 못 읽는다. 툴팁은 마우스를 올려야 나와 대안이 못 된다 |
| 상태 표시 | **색 + 글자/모양** | 색만 쓰면 색약 아이와 밝은 교실 화면에서 안 보인다 |

지금 화면에서 이 기준에 걸리는 것 — 헤더 알약 단추 높이 30px(→48), `?` 단추 30×30px(→48),
패널 머리표 단추 상하 패딩 6px(실높이 ≈30px), `.hint`·로그·기록 캡션 글씨 13px 대.
**재설계에서 한꺼번에 올린다.**

### 2.1 셸 — 레일 + 타이틀바

```
┌───────────────────────────────────────────────────────────┐
│ edge-lab │ 파이썬            [ ? ]  [ 한/영 ]  [ NPU 41ms ] │ titlebar 40px
├──────┬────────────────────────────────────────────────────┤
│ ⌂ 홈 │                                                    │
│ ◉ 체험│                                                    │
│ ▦ 블록│           workbench — 남는 공간 전부               │
│ ▶ 파이썬                                                   │
│ ✎ 가르치기                                                 │
│ ✆ 대화│                                                    │
│      │                                                    │
│ ⚙ 설정│                                                    │
└──────┴────────────────────────────────────────────────────┘
  rail 76px
```

- **rail (좌 76px)** — 홈 · 체험 · 블록 · 파이썬 · 가르치기 · 대화 · 설정.
  칸 하나가 **68 × 56 px**(아이콘 26px + 글자 12px)로 48px 기준을 넘긴다.
  **아이콘 아래 글자를 늘 붙인다** — 툴팁으로 대신하지 않는다(§2.0).
  세로라서 **절대 줄바꿈하지 않는다.** 지금 화면은 배경 + 왼쪽 굵은 띠로 표시(색만 아님).
  좁은 화면(<640px)에서는 화면 하단 가로 바로 내려간다.
- **titlebar (40px)** — 왼쪽 `edge-lab` + 지금 앱 이름, 오른쪽 도구(`?` · `한/영` · `AI 상태`).
  도구 단추는 **높이 28px, 클릭 영역 40px**(투명 패딩으로 넓힌다).
  지금 `.header` 를 **낮추기만 한 것**이라 sysbar·tour 는 선택자만 갈아끼우면 산다.
- **workbench** — 페이지 내용. `.main-container` 를 그대로 옮긴다. 페이지 CSS 변경 최소.

**세로 크롬 회수 — 체험하기 기준 183px → 61px.** 768px 화면에서 내용 높이 585px → 707px (+21%).
가로 76px 을 레일에 내주지만, 탭 줄바꿈(+42px)이 원천적으로 사라지고
아이가 누를 표적이 30px 에서 56px 로 커진다.

레일에 **글자를 붙이므로** `nav.js` 의 "탭은 글자만" 제약(§1.6)과 정면으로 부딪히지는 않는다 —
글자는 그대로 있고 방향만 가로에서 세로로 눕는다. 다만 파이보 랩·센스 랩과 나란히 놓았을 때
모양이 달라지는 것은 사실이라 **1번 결정 항목**으로 남긴다.

### 2.2 런처 — 웹OS 홈

새 페이지 `home.html`, 라우트 `/home`. 두 구역.

**① 앱 (6장)** — 큰 타일. 체험 · 블록 · 파이썬 · 가르치기 · 대화 · 설정.
아이콘 44px + 이름 16px + 한 줄 설명 13px. **타일 높이 80px 이상**, `minmax(230px, 1fr)`.
타일 사이 간격 12px — 붙여 놓으면 옆 앱이 열린다.

**② AI 앱 (16장)** — `SERVICE_LIST` 를 그대로 타일로 편다.
지금 `<select>` + `optgroup` 6묶음(얼굴 6 · 몸·손 2 · 사물 2 · VLM 1 · 사진 3 · 글자 2)을
**묶음 머리표 + 아이콘 그리드**로 푼다. 타일을 누르면
`/try?m=<key>` 로 체험하기가 그 모델이 골라진 채로 열린다.
드롭다운은 유지하되(다른 모델로 바꾸기), 첫 선택은 런처가 맡는다.

- 타일: 아이콘 52px + 이름 16px + 곁글(도는 장치·모델) 12px. **높이 112px 이상.**
- 그리드: `repeat(auto-fill, minmax(180px, 1fr))`, 간격 12px —
  1920px 에서 9열, 1366px 6열, 태블릿 4열, 폰 2열.
  **미디어 쿼리 없이 폭에 자동으로 맞는다.**
- 이름은 **줄여 쓰지 않는다.** 말줄임(`text-overflow: ellipsis`)으로 "얼굴 나이/성별 분…" 이
  되면 아이는 무엇인지 모른다 — 두 줄로 흘리고 타일 높이를 늘린다.
- 아이콘: FA Free 웹폰트. 매핑 예 —
  얼굴 `fa-face-smile` · 사물 `fa-cube` · 손 `fa-hand` · 포즈 `fa-person-walking` ·
  VLM `fa-comment-dots` · 깊이 `fa-layer-group` · 글자 `fa-font` · QR `fa-qrcode` ·
  가르치기 `fa-graduation-cap` · 블록 `fa-puzzle-piece` · 파이썬 `fa-code` · 대화 `fa-comments`.
  **아이콘은 글자를 돕는 것이지 대신하는 것이 아니다** — 타일에는 늘 이름이 함께 있다.
- 묶음 머리표(얼굴 · 몸·손 · 사물 …)는 **글자 15px**. 아이가 "얼굴 쪽에서 고른다" 로 찾는 길이다.
- "내가 가르친 AI" 는 `/custom/list` 결과를 별도 줄로 붙인다(있을 때만).
- `usage.js` 가 이미 페이지별 연 횟수를 세므로, **자주 쓴 앱** 줄을 맨 위에 둘 수 있다.

**모델 목록의 단일 출처** — `SERVICE_LIST`/`GROUPS` 를 `index.html` 인라인에서 빼
`lib/apps.js` 로 옮기고 런처와 체험하기가 같이 읽는다. 지금은 index 안에만 있어
런처를 만들면 두 벌이 된다.

### 2.3 반응형 — 기준 한 벌

`lib/tokens.css` 에 브레이크포인트를 변수로 두고 전 페이지가 같은 값을 쓴다.

| 이름 | 폭 | 배치 |
|---|---|---|
| `sm` | < 640 | 레일 → 하단 바. 패널 1열. 문서 스크롤. |
| `md` | < 1024 | 패널 세로 적층. 문서 스크롤. |
| `lg` | < 1440 | 좌우 분할 유지. 교실 노트북 기준. |
| `xl` | ≥ 1440 | 분할 유지 + 여백 한 단계 넉넉히. |

- **`md` 미만은 전 페이지가 `overflow:auto; height:auto`.** 지금 blocks 만 그렇다.
- **좁아질 때 글씨를 줄이지 않는다.** 지금 `--sp-*` 가 `clamp(…vh…)` 로 화면 높이를 따라
  줄어드는데, 여백만 줄이고 **글씨·표적 크기는 §2.0 최소값 아래로 내려가지 않게 막는다.**
  노트북에서 촘촘해지는 것과 아이가 못 누르는 것은 다른 문제다.
- 패널 `min-width` 하드코딩 8종 → `clamp(240px, 22vw, 420px)` 한 식으로.
- 패널 내부 접힘은 **컨테이너 쿼리(`@container`)** 로. 스플리터로 좁힌 패널도 알아서 접힌다
  (미디어 쿼리는 창 폭만 보므로 스플리터에 반응 못 한다).
  `main.py:_find_app_browser()` 가 설치된 Chrome 또는 Edge 를 앱 모드로 띄운다 — 둘 다
  Chromium 이고 자동 갱신되므로 컨테이너 쿼리(Chromium 105+, 2022-08)는 안전하다.
  다만 둘 다 없으면 `webbrowser.open()` 으로 기본 브라우저에 넘어가므로,
  **컨테이너 쿼리가 없을 때 미디어 쿼리로 떨어지는 대비를 함께 둔다.**

### 2.4 브랜드

- 화면 문자열 22곳 전부 `edge-lab` (한/영 동일, 소문자 하이픈).
  서버는 이미 `FastAPI(title="edge-lab")` 이고 콘솔도 `! edge-lab listening on ...` 이다 —
  화면만 한글이라 어긋나 있었다.
- `<title>` 형식: `edge-lab — 파이썬` / `edge-lab — Python`.
- 부팅화면 `<b>` 도 `edge-lab`, 아래 서브카피(`인터넷 없이 내 컴퓨터에서 실행돼요`)는 한국어 유지.
- README·INSTALL·DEVELOP 의 "엣지 랩" 은 문서라 그대로 둔다(문장 안에 녹아 있음).
- `themaker.py`·`tools/*` 는 **결정 항목 4번.**

---

## 3. 구현 단계

각 단계가 끝날 때마다 프로그램이 도는 상태를 유지한다. 단계별로 커밋을 나눈다.

| # | 내용 | 건드리는 파일 | 회귀 위험 |
|---|---|---|---|
| 1 | **토큰 분리** — `lib/tokens.css` 신설, 6페이지 인라인 `:root`·`html.hc` 삭제 | 6 HTML + 신규 1 | 낮음 (값 동일) |
| 2 | **셸** — `lib/shell.css` + `lib/shell.js`. `nav.js` → 레일 생성기(아이콘+글자). `.header` → `.titlebar`. 헤더 단추 30px → 클릭영역 40px. sysbar `top:56px` · tour 선택자 갱신 | 6 HTML + lib 4 | **중간** |
| 3 | **앱 목록 분리** — `lib/apps.js` (SERVICE_LIST·GROUPS·앱 6종·아이콘 매핑) | index + 신규 1 | 낮음 |
| 4 | **런처** — `home.html`, `main.py` 라우트, FA Free 연결 | 신규 1 + main.py | 낮음 |
| 5 | **체험하기 재배치** — 문항1·2 두 막대를 패널 머리표로 흡수, `?m=` 파라미터 수신 | index | 중간 |
| 6 | **반응형 통일** — 브레이크포인트 변수, `min-width` → `clamp`, 컨테이너 쿼리 | 6 HTML + tokens | 중간 |
| 6b | **최소 치수 정리** — 전 페이지 `.hint`·로그·캡션 13px 대 → 14px, 단추 실높이 30px → 48px | 6 HTML + ui.css | 낮음 |
| 7 | **브랜드 치환** — 22곳 | 6 HTML | 낮음 |
| 8 | **아이콘 정리** — FA Free 적용, Pro 헤더 SVG 47개 삭제 | assets | 낮음 |
| 9 | **튜토리얼 갱신** — `tour.js` 단계에서 `.nav-tab#blocksLink` 등 죽은 선택자 교체, 런처 단계 추가 | tour.js | 낮음 |

점검은 `tools/e2e/pages.js` 와 `tools/schema_test/run.bat` 으로.

**`tools/e2e/pages.js` 는 2단계에서 반드시 같이 고쳐야 한다** — 여섯 페이지의
`.header .h-tools > a, .header .h-tools > button` 개수를 세어 "헤더단추" 로 보고한다.
`.header` → `.titlebar` + 레일로 바뀌면 이 값이 전부 0 이 되어 시험이 통과한 척 조용히 망가진다.
`lib/sysbar.js` 의 `#sysPanel{top:56px}` 도 새 타이틀바 높이(34px)에 맞춰야 하고,
`lib/tour.js` 의 `.nav-tab#blocksLink` 단계는 레일 선택자로 갈아야 한다(9단계).

---

## 4. 확인이 필요한 것

| # | 질문 | 왜 중요한가 |
|---|---|---|
| 1 | 세로 레일(아이콘 + 글자, 76px)로 갈 것인가? `nav.js` 는 "파이보 랩·센스 랩과 같은 글자 탭"을 명시적 제약으로 적어 두었다. 레일도 글자를 유지하지만 방향이 다르다 | 2단계 전체가 갈린다. 세 제품 정합성 문제 |
| 2 | `/` 를 런처로 바꾸고 체험하기를 `/try` 로 옮기는가, 아니면 `/` 는 그대로 두고 `/home` 을 더하는가 | 북마크·튜토리얼·`tools/e2e` 경로가 따라 움직인다 |
| 3 | 런타임 브라우저는 무엇인가 (`tools/launcher.py` 가 띄우는 창). Chromium 105+ 면 컨테이너 쿼리를 쓴다 | 6단계 방식이 갈린다 |
| 4 | `themaker.py`·`tools/*.bat` 의 "엣지 랩" 도 `edge-lab` 으로 바꾸는가 | 학생이 보는 예외 메시지다 |

---

## 5. 하지 않을 것

- **프레임워크 도입 안 함.** React·Vue·번들러 없이 지금처럼 정적 HTML + 바닐라 JS 로 간다.
  오프라인 설치 배포이고 빌드 단계를 늘리면 `setup_deploy.ps1` 이 무거워진다.
- **페이지 파일 분해 안 함.** 85~175KB 단일 파일 6개를 컴포넌트로 쪼개는 것은 이번 범위 밖이다.
  공용으로 빼는 것은 토큰·셸·앱 목록 셋뿐.
- **색·글꼴 안 바꿈.** "학습지" 톤(Gowun Batang/Dodum, `--paper #fbf7ef`, `--cyan #1f5f7a`)과
  파이보 캐릭터·말풍선·"참 잘했어요" 도장은 그대로 둔다. 아이가 이미 아는 화면이고,
  이번 작업은 **배치와 치수**만 바꾼다.
- **아이 화면을 어른 화면처럼 촘촘하게 만들지 않는다.** 공간은 크롬을 없애서 얻지,
  글씨와 단추를 줄여서 얻지 않는다 — §2.0 이 그 선이다.

---

## 6. 프로토타입

배치와 치수를 실제로 돌려 본 것 — 화면 폭 단추(1920 / 1366 / 1024 / 768 / 400)로 반응을 확인한다.
컨테이너 쿼리가 브라우저 창이 아니라 **그 화면 폭**에 반응하는 것이 §2.3 방식의 증거다.

<https://claude.ai/code/artifact/80185202-e82c-45e2-8662-ab6e22fe8fe4>

담긴 화면 — 홈(런처) · 체험하기 · 파이썬. 색·글꼴은 현행 토큰 그대로이고 배치와 치수만 다르다.
